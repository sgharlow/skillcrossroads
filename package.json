{
  "name": "beacon-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Skill Crossroads — the signpost for Claude Code artifacts. An evidence-based quality scorecard, letter grade, and embeddable badge for Skills, subagents, MCP servers, and plugins: ship, fix, or rethink before you release.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build packages/cli/tsconfig.json && npm run typecheck --workspace @beacon/web",
    "lint": "npm run typecheck",
    "eval:triggering": "node scripts/eval-triggering.mjs",
    "eval:tokens": "node scripts/eval-token-accuracy.mjs",
    "report:skills": "node scripts/state-of-skills.mjs",
    "report:web": "node -e \"const fs=require('fs');const md=fs.readFileSync('reports/state-of-claude-code-skills.md','utf8');fs.writeFileSync('apps/web/content/report.ts','// GENERATED - do not edit by hand. Source: reports/state-of-claude-code-skills.md\\n// Regenerate: npm run report:web   (after npm run report:skills regenerates the source)\\nexport const REPORT_MD: string = '+JSON.stringify(md)+';\\n')\"",
    "beacon": "node packages/cli/dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.7"
  },
  "license": "MIT"
}
