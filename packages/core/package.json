{
  "name": "@beacon/core",
  "version": "0.1.0",
  "description": "Beacon's rule engine: parse ▸ run checks ▸ score ▸ render. Model-agnostic, deterministic core shared by the CLI, web app, and CI Action.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "picocolors": "^1.1.0",
    "yaml": "^2.6.0"
  },
  "license": "MIT"
}
